services:
  application:
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - .:/home/app
    env_file:
      - .env.example
    environment:
      - IS_DOCKER=True
